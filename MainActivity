package com.example.dynamicfeatures

import android.content.Intent
import android.os.Bundle
import android.widget.Button
import androidx.appcompat.app.AppCompatActivity

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        val buttonAddFeature = findViewById<Button>(R.id.button_add_feature)
        buttonAddFeature.setOnClickListener {
            // Uruchamiamy nową funkcję (moduł)
            val intent = Intent(this, NewFeatureActivity::class.java)
            startActivity(intent)
        }
    }
}
